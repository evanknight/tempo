export async function exchangeToken(code: string) {
  // Placeholder for Strava OAuth exchange
  return { access_token: 'stub' };
}

export async function fetchActivities(token: string) {
  // Placeholder to fetch activities
  return [];
}
